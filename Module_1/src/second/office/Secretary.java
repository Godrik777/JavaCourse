package second.office;

public class Secretary {
    public void secretaryRage(Boss boss, Manager manager, Guard guard) {
        System.out.println(boss.name + " не волнуйтесь, " + manager.name + " все успеет. " + guard.name + " - подождите!");
    }
}
